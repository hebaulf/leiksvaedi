/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'www.pngkey.com', 'reykjavik.is'],
  },
}

module.exports = nextConfig
